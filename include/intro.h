#ifndef INTRO_H
#define INTRO_H

// Show a simple intro / start screen. This blocks until the player
// presses Enter or Space (or the window is closed). Returns when the
// player requests the game to start.
void ShowIntroScreen(void);

#endif // INTRO_H
